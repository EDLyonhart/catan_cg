<% provide(:title, 'New Game') %>

<% unless Game.users_by_name(@game.id).size == 2 %>

  <%= form_for :player do |f| %>
    Player 1 : <%= select_tag "Player 1", options_for_select(Game.users_by_name), {:include_blank => 'select player'} %><br />
    <%= f.submit %>
  <% end %>
<% end %>